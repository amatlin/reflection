# Step 2: The Loop / Stream

## Current heading
the loop

## Current copy
When you click the button below, a `fire_event` is captured by the analytics tracker, sent to the backend, stored in the database, and broadcast to every connected visitor.

Watch the stream.

## New heading
event stream

## New copy

when you click the button below, a `fire_event` is recorded by an event listener and added to the stream, associated with your unique ID. 

your data flows in a gentle current, fresh like spring water.